import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'
import ASlider from './a-slider.vue';

<Meta title="Design System/Atoms/ASlider" component={ASlider} />

## ASlider

### Default use:

<Canvas>
  <Story name="Default use">
    {{
        components: { ASlider },
        data() {
          return {
            value: 10,
          };
        },
        methods: {
          handleSlider(value) {
            this.value = value;
          },
        },
        template: `
        <div>
          <a-slider :value="value" :min="0" :max="100" :step="1" @change="handleSlider" />
        </div>
        `,
    }}
  </Story>
</Canvas>

<ArgsTable of={ASlider} />
