import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions'
import HSelect from './select.vue';

<Meta title="Design System/Atoms/Select" component={HSelect} />

## Select

### Default use:

<Canvas>
  <Story name="Default use">
    {{
        components: { HSelect },
        data() {
          return {
            selected: 'example1',
          };
        },
        template: `
        <div style="height: 40vh;">
          <h-select
            v-model="selected"
            :options="['example1', 'Example 1']"
          />
          {{ selected }}
        </div>
        `,
    }}
  </Story>
</Canvas>

### With listenners use:

<Canvas>
  <Story name="With listenners use">
    {{
        components: { HSelect },
        data() {
          return {
            selected: '',
          };
        },
        template: `
        <div style="height: 40vh;">
          <h-select
            @select="selected = $event"
            :value="selected"
            :options="['example1', 'Example 1']"
          />
          <h-select
            @input="selected = $event"
            :value="selected"
            :options="['example1', 'Example 1']"
          />
          <h-select
            @change="selected = $event"
            :value="selected"
            :options="['example1', 'Example 1']"
          />
          {{ selected }}
        </div>
        `,
    }}
  </Story>
</Canvas>

### Use with object with attrs, value and text:

<Canvas>
  <Story name="Use with object with attrs, value and text">
    {{
        components: { HSelect },
        data() {
          return {
            selected: '',
          };
        },
        template: `
        <div style="height: 40vh;">
          <h-select
            v-model="selected"
            placeholder="Blah"
            :options="[{value: 'example1', text: 'EXAMPLE 1'}, { value: 'data1', text: 'DATA 1'}]"
          />
          {{ selected }}
        </div>
        `,
    }}
  </Story>
</Canvas>

### placeholder:

<Canvas>
  <Story name="placeholder">
    {{
        components: { HSelect },
        template: `
        <div>
          <h-select placeholder="placeholder" :options="['yes', 'no']" />
        </div>
        `,
    }}
  </Story>
</Canvas>

### Sizes:

<Canvas>
  <Story name="Sizes">
    {{
        components: { HSelect },
        template: `
        <div>
          <h-select placeholder="small" size="small" />
          <p />
          <h-select placeholder="medium" size="medium" />
        </div>
        `,
    }}
  </Story>
</Canvas>

### Behavior:

<Canvas>
  <Story name="Behavior">
    {{
        components: { HSelect },
        template: `
        <div>
          <h-select placeholder="default" behavior="default" />
          <p />
          <h-select placeholder="block" behavior="block" />
        </div>
        `,
    }}
  </Story>
</Canvas>

<ArgsTable of={HSelect} />
