import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'
import AButton from './a-button.vue';

<Meta title="Design System/Atoms/AButton" component={AButton} />

## AButton

### Default use:

<Canvas>
  <Story name="Default use">
    {{
        components: { AButton },
        template: '<a-button @click="action">Hello Button</a-button>',
        methods: { action: action("clicked") }
    }}
  </Story>
</Canvas>

### Variants:

<Canvas>
  <Story name="Variants">
    {{
        components: { AButton },
        template: `
        <div>
          <a-button variant="primary">primary</a-button>
          <a-button variant="secondary">secondary</a-button>
          <a-button variant="tertiary">tertiary</a-button>
          <a-button variant="quaternary">quaternary</a-button>
          <a-button variant="quintenary">quintenary</a-button>
        </div>
        `,
        methods: { action: action("clicked") }
    }}
  </Story>
</Canvas>

### With icon:

<Canvas>
  <Story name="With icon">
    {{
        components: { AButton },
        template: `
        <div>
          <a-button @click="action" icon="fas fa-home">Hello Button</a-button>
          <a-button @click="action" icon="fas fa-user">Hello Button</a-button>
        </div>
        `,
        methods: { action: action("clicked") }
    }}
  </Story>
</Canvas>

### Sizes:

<Canvas>
  <Story name="Sizes">
    {{
        components: { AButton },
        template: `
        <div>
          <a-button @click="action" size="small">Small</a-button>
          <a-button @click="action" size="medium">Medium</a-button>
        </div>
        `,
        methods: { action: action("clicked") }
    }}
  </Story>
</Canvas>

### Rounded:

<Canvas>
  <Story name="Rounded">
    {{
        components: { AButton },
        template: `
        <div>
          <a-button @click="action" :is-rounded="true">Rounded</a-button>
          <a-button @click="action" :is-rounded="false">Not rounded</a-button>
        </div>
        `,
        methods: { action: action("clicked") }
    }}
  </Story>
</Canvas>

### Loading:

<Canvas>
  <Story name="Loading">
    {{
        components: { AButton },
        template: `
        <div>
          <a-button @click="action" :is-loading="true">Loading</a-button>
        </div>
        `,
        methods: { action: action("clicked") }
    }}
  </Story>
</Canvas>

### Only Icon:

<Canvas>
  <Story name="Only Icon">
    {{
        components: { AButton },
        template: `
        <div>
          <a-button @click="action" icon="fas fa-user" />
        </div>
        `,
        methods: { action: action("clicked") }
    }}
  </Story>
</Canvas>

### Behavior:

<Canvas>
  <Story name="Behavior">
    {{
        components: { AButton },
        template: `
        <div>
          <a-button @click="action" behavior="block">Behavior</a-button>
        </div>
        `,
        methods: { action: action("clicked") }
    }}
  </Story>
</Canvas>

### All html attrs and disabled:

<Canvas>
  <Story name="All html attrs and disabled">
    {{
        components: { AButton },
        template: `
        <div>
          <a-button id="my-specific-component">Behavior</a-button>
          <a-button disabled>Behavior</a-button>
        </div>
        `,
    }}
  </Story>
</Canvas>

<ArgsTable of={AButton} />
