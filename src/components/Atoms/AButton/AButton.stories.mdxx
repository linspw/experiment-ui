import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'
import { linkTo } from '@storybook/addon-links'
import AButton from './AButton.vue';

<Meta title="MDX|Button" component={AButton} />

# Button

<ArgsTable of={AButton} />

This is a simple button with some text in it.

<Canvas>
  <Story name="With Text">
    {{
        components: { AButton },
        template: '<a-button @click="action">Hello Button</a-button>',
        methods: { action: action("clicked") }
    }}
  </Story>
</Canvas>

You can perform some action when the button is clicked.

<Canvas>
  <RAButton onClick={linkTo('Button', 'With Some Emoji')}>With JSX</RAButton>
</Canvas>

You can even have Emoji in the button.

<Canvas>
  <Story name="With Some Emoji">
    {{
        components: { AButton },
        template: '<a-button>😀 😎 👍 💯</a-button>'
    }}
  </Story>
</Canvas>
