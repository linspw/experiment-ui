import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions'
import HDropdown from './dropdown.vue';

<Meta title="Design System/Atoms/Dropdown" component={HDropdown} />

## Dropdown

### Default use:

<Canvas>
  <Story name="Default use">
    {{
        components: { HDropdown },
        data() {
          return {
            selected: 'placeholder',
          };
        },
        template: `
        <div style="height: 40vh;">
          <h-dropdown
            v-model="selected"
            :options="['example1', 'Example 1']"
          />
          {{ selected }}
        </div>
        `,
    }}
  </Story>
</Canvas>

### Only select option use:

<Canvas>
  <Story name="Only select option use">
    {{
        components: { HDropdown },
        data() {
          return {
            selected: 'blah',
          };
        },
        template: `
        <div style="height: 40vh;">
          <h-dropdown
            label="Menu"
            @select="selected = $event"
            :options="['example1', 'Example more 1000 th']"
          />
          {{ selected }}
        </div>
        `,
    }}
  </Story>
</Canvas>

### Variants use:
Accepts all HButton variants

<Canvas>
  <Story name="Variants use">
    {{
        components: { HDropdown },
        data() {
          return {
            selected: 'blah',
          };
        },
        template: `
        <div style="height: 40vh;">
          <h-dropdown
            label="Menu"
            @select="selected = $event"
            :options="['example1', 'Example 1']"
            variant="primary"
          />
          <br>
          <h-dropdown
            label="Menu"
            @select="selected = $event"
            :options="['example1', 'Example 1']"
            variant="quaternary"
          />
          {{ selected }}
        </div>
        `,
    }}
  </Story>
</Canvas>

### Position use:

<Canvas>
  <Story name="Position use">
    {{
        components: { HDropdown },
        data() {
          return {
            selected: 'blah',
          };
        },
        template: `
        <div style="height: 40vh;">
          <h-dropdown
            label="Menu"
            @select="selected = $event"
            position="default"
            :options="['example1', 'Example more 1000 th']"
          />
          <br>
          <h-dropdown
            label="Menu"
            @select="selected = $event"
            position="centered"
            :options="['example1', 'Example more 1000 th']"
          />
          {{ selected }}
        </div>
        `,
    }}
  </Story>
</Canvas>

<ArgsTable of={HDropdown} />
