import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions'
import ADropdown from './a-dropdown.vue';

<Meta title="Design System/Atoms/ADropdown" component={ADropdown} />

## ADropdown

### Default use:

<Canvas>
  <Story name="Default use">
    {{
        components: { ADropdown },
        data() {
          return {
            selected: 'placeholder',
          };
        },
        template: `
        <div style="height: 40vh;">
          <a-dropdown
            v-model="selected"
            :options="['example1', 'Example 1']"
          />
          {{ selected }}
        </div>
        `,
    }}
  </Story>
</Canvas>

### Only select option use:

<Canvas>
  <Story name="Only select option use">
    {{
        components: { ADropdown },
        data() {
          return {
            selected: 'blah',
          };
        },
        template: `
        <div style="height: 40vh;">
          <a-dropdown
            label="Menu"
            @select="selected = $event"
            :options="['example1', 'Example more 1000 th']"
          />
          {{ selected }}
        </div>
        `,
    }}
  </Story>
</Canvas>

### Variants use:
Accepts all AButton variants

<Canvas>
  <Story name="Variants use">
    {{
        components: { ADropdown },
        data() {
          return {
            selected: 'blah',
          };
        },
        template: `
        <div style="height: 40vh;">
          <a-dropdown
            label="Menu"
            @select="selected = $event"
            :options="['example1', 'Example 1']"
            variant="primary"
          />
          <br>
          <a-dropdown
            label="Menu"
            @select="selected = $event"
            :options="['example1', 'Example 1']"
            variant="quaternary"
          />
          {{ selected }}
        </div>
        `,
    }}
  </Story>
</Canvas>

### Position use:

<Canvas>
  <Story name="Position use">
    {{
        components: { ADropdown },
        data() {
          return {
            selected: 'blah',
          };
        },
        template: `
        <div style="height: 40vh;">
          <a-dropdown
            label="Menu"
            @select="selected = $event"
            position="default"
            :options="['example1', 'Example more 1000 th']"
          />
          <br>
          <a-dropdown
            label="Menu"
            @select="selected = $event"
            position="centered"
            :options="['example1', 'Example more 1000 th']"
          />
          {{ selected }}
        </div>
        `,
    }}
  </Story>
</Canvas>

<ArgsTable of={ADropdown} />
