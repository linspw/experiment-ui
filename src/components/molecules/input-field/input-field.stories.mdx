import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions'
import { HInput } from '@components/atoms/input';
import HInputField from './input-field.vue';

<Meta title="Design System/Molecules/InputField" component={HInputField} />

## InputField

### Uso normal:

<Canvas>
  <Story name="Uso normal">
    {{
        components: { HInputField, HInput },
        template: `
        <h-input-field label="Example" helper-text="Message">
          <h-input type="text" v-model="example" />  
        </h-input-field>
        `,
        data(){
          return {
            example: 'Text',
          };
        },
    }}
  </Story>
</Canvas>

### Mensagem de error:

<Canvas>
  <Story name="Mensagem de error">
    {{
        components: { HInputField, HInput },
        template: `
        <h-input-field label="Example" :has-error="true" helper-text="Error message">
          <h-input type="text" v-model="example" />  
        </h-input-field>
        `,
        data(){
          return {
            example: 'Validation',
          };
        },
    }}
  </Story>
</Canvas>


<ArgsTable of={HInputField} />
